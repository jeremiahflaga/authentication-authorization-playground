@page
@model SampleSite.Pages.Page3Model
@{
    ViewData["Title"] = "Page 3";
}

<h1>Page 3</h1>

<hr />

<div>
    <h2>User</h2>
    @foreach (var identity in User.Identities)
    {
        <div>Authentication Type: @identity.AuthenticationType</div>
        <table class="table">
            @foreach (var claim in identity.Claims)
            {
                <tr>
                    <td>@claim.Type</td>
                    <td>@claim.Value</td>
                </tr>
            }
        </table>
    }
</div>